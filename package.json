{
  "main": "main.js",
  "engines": {
    "node": "16.14.2"
  },
  "dependencies": {
    "@tailwindcss/line-clamp": "^0.4.0",
    "cross-env": "7.0.3",
    "dotenv": "16.0.0",
    "express": "4.18.1",
    "formidable": "2.0.1"
  },
  "devDependencies": {
    "nodemon": "2.0.16",
    "tailwindcss": "^3.0.24"
  },
  "scripts": {
    "start": "npm i && cross-env DEPLOY_MODE=true npx pm2 start main.js -i max --attach",
    "devstart": "npm i && nodemon -e js main.js",
    "clearDb": "node clearDb.js"
  },
  "nodemonConfig": {
    "ignore": [
      "public"
    ]
  }
}
